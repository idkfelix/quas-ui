import Spinner from "./button-spinner.svelte";
import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

export {
	Root,
	Spinner,
	type ButtonProps as Props,
	//
	Root as Button,
	Spinner as ButtonSpinner,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
